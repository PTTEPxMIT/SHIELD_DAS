// Supabase project coordinates for the live viewer.
//
// Fill these in from Project Settings -> API. The anon key is PUBLIC BY
// DESIGN: row-level security makes it read-only, and the data it can read
// is already published in SHIELD-Data. Never put the service_role key here.
window.SHIELD_LIVE_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
};
